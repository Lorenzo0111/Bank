model User {
    id        String   @id @default(cuid())
    name      String
    surname   String
    birthdate DateTime

    email    String @unique
    password String

    balance Decimal @default(0)

    @@map("users")
}
